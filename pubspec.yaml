name: google_places_sdk_flutter
description: "Cross-platform Google Places autocomplete widgets and clients built on Places API (New)."
version: 0.3.0
homepage: https://github.com/guy-evdev/google_places_autocomplete
repository: https://github.com/guy-evdev/google_places_autocomplete
topics:
  - flutter
  - autocomplete
  - google-maps
  - places

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - assets/google_black.png
    - assets/google_white.png
